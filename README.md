# HeartLight — Emotion → Ayah (Flutter)

## Run
```
flutter pub get
flutter run
```

Type an emotion like `sad`, `anxious`, `guilty`, `lonely`, `rizq`, `patience`.
Add more entries in `assets/quran_emotion_map.json`.
